const FIELD_TYPES = {
  STRING: 'string',
  BOOLEAN: 'boolean',
  NUMBER: 'number',
  DATE: 'date',
  MIXED: 'mixed',
  UUID: 'uuid',
  IMAGE: 'image',
  VIDEO: 'video',
  MEDIA: 'media',
  LINK: 'link'
}

const FILTER_TYPES = {
  DISCRETE: 'DISCRETE',
  RANGE: 'RANGE',
  DATE: 'DATE',
  TEXT: 'TEXT'
}

module.exports = {
  FIELD_TYPES,
  FILTER_TYPES
}
